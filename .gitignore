# 환경변수
back/.env.production
back/.env

# SQLite
back/*.db
back/*.db-shm
back/*.db-wal

# 전략 문서 (비공개)
docs/STRATEGY.md

# Firebase 서비스 계정 키 (절대 커밋 금지)
back/firebase-service-account.json
back/firebase-service-account*.json
