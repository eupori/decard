name: decard
description: "PDF → 근거 포함 암기카드 생성기"
publish_to: 'none'
version: 0.1.0+1

environment:
  sdk: ^3.11.0

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  http: ^1.2.0
  file_picker: ^8.0.0
  google_fonts: ^6.2.0
  url_launcher: ^6.2.0
  share_plus: ^10.0.0
  path_provider: ^2.1.0
  shared_preferences: ^2.3.0
  uuid: ^4.5.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
